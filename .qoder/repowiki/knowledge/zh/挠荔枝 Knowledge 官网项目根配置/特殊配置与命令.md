开发：`npm run dev`；构建：`npm run build`（先执行 `tsc -b` 再 `vite build`）；预览：`npm run preview`；lint：`npm run lint`。shadcn/ui 组件通过 CLI 安装到 `@/components/ui`，需确保 `postcss.config.js` 与 `src/index.css` 已按 `components.json` 初始化。