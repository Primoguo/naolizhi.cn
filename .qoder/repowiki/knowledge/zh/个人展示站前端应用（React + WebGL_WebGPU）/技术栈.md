React 19 + Vite 7 + TypeScript 5.9；样式采用 Tailwind CSS 3 + shadcn/ui（Radix UI primitives + class-variance-authority + tailwind-merge）；图形方面同时使用 WebGL（`OES_texture_half_float` 扩展实现流体模拟）与 WebGPU（`@webgpu/types` + `requestAdapter/requestDevice`），TTS 演示依赖浏览器原生 SpeechSynthesis。